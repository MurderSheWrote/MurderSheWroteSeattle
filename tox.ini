[tox]
envlist = py27, py35

[testenv]
commands = py.test {posargs}
deps = 
    pyramid
    pytest
    pytest-cov
    webtest
    sodapy
passenv =
    TESTING_URL
    DATABASE_URL
    SOCRATA_TOKEN

[pytest]
addopts = --ignore=lib64 --ignore=lib